version: "3.7"

services:
  runner:
    image: myoung34/github-runner:latest
    restart: always
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      RUNNER_SCOPE: repo
      RUNNER_NAME_PREFIX: myrunner
      LABELS: some-label
      REPO_URL: https://github.com/linhvq14/cicd-setup
      EPHEMERAL: 1
      RUNNER_TOKEN: AJS47FHK2BMULA3R3624ZN3F5ZYHC
      RUNNER_WORKDIR: /tmp/runner/work
      ORG_RUNNER: 'false'
    deploy:
      replicas: 3
      resources:
        limits:
          cpus: '1'
          memory: 1G
        reservations:
          cpus: '0.2'
          memory: 256M
